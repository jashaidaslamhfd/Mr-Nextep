diff --git a/.github/workflows/analytics.yml b/.github/workflows/analytics.yml
index 123abc..456def 100644
--- a/.github/workflows/analytics.yml
+++ b/.github/workflows/analytics.yml
@@ -35,6 +35,7 @@ jobs:
         run: python src/analytics_updater.py
 
       - name: Update Facebook Reels metrics (best effort)
+        if: always()
         env:
           FB_ACCESS_TOKEN: ${{ secrets.FACEBOOK_ACCESS_TOKEN }}
           FB_API_VERSION: "v23.0"
@@ -43,6 +44,7 @@ jobs:
         run: python scripts/update_facebook_analytics.py
 
       - name: Persist analytics state
+        if: always()
         run: |
           set -euo pipefail
           if [ -z "$(git status --porcelain -- data/)" ]; then

# FIX REASON (2026-07-31):
# YouTube Analytics API is currently DISABLED (403) for project 559439687452.
# Old workflow: YouTube step fails with exit 2 -> FB metrics step never runs -> growth report never commits -> channel stays blind on all platforms.
# New: FB step + persist run always(), so even when YT is disabled, Meta metrics still collect and report shows exact blocker.
# Also src/analytics_updater.py now exits 0 (not 2) on api_disabled, logging fix URL:
# https://console.developers.google.com/apis/api/youtubeanalytics.googleapis.com/overview?project=559439687452
