# Pinned exactly. Floating >= specifiers on a job that runs unattended three times a
# day mean an upstream release can take production down with no change on our side.
# Dependabot (.github/dependabot.yml) proposes upgrades as reviewable PRs instead.
Pillow==12.3.0
python-dotenv==1.1.1
google-api-python-client==2.200.0
google-auth==2.41.1
google-auth-oauthlib==1.2.2
edge-tts==7.2.8
requests==2.34.2
pytest==8.4.2
pydantic==2.13.5
pydantic-settings==2.15.0
